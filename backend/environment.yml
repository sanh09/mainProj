name: appenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - openai
      - requests
      - uvicorn[standard]
      - python-dotenv
      - psycopg[binary]
      - python-multipart
      - email-validator
